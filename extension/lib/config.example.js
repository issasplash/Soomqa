// Copy this file to `config.js` and fill in your API keys.
// `config.js` is gitignored so secrets never get committed.
//
// How to get the keys:
//   ETHERSCAN_API_KEY — https://etherscan.io/myapikey (free tier: 5 req/sec)
//   ALCHEMY_API_KEY   — https://dashboard.alchemy.com (free tier: 300M compute units/mo)
//
// One Alchemy key works for all EVM chains we support (Ethereum, Base, Arbitrum, Optimism).
// Etherscan key works for Etherscan + Basescan + Arbiscan + Optimistic Etherscan
// only if you upgrade to the unified Etherscan V2 API key (free).

export const config = {
  ETHERSCAN_API_KEY: "YOUR_ETHERSCAN_KEY_HERE",
  ALCHEMY_API_KEY: "YOUR_ALCHEMY_KEY_HERE",
};
